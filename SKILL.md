---
name: s2-orchestration
description: 长任务 S2 多 Agent 编排执行。当任务预计 >5 分钟、需多源调研、需生成/重构文件（docx/xlsx/pptx/pdf/md）或需拆分多个子任务并行时触发。用「可靠任务书 + 结果落盘 + 查文件等待 + 分块读写 + 断点续传」把任务拆成并行子 agent 跑完，避免卡死、丢结果、图片化中断、重复交付。触发关键词：长任务、多Agent、多agent、拆分、调研、演讲稿、报告、可研、方案、编排、S2、落盘、断点续传、图片化、并行、子任务。
---

# 长任务 S2 多 Agent 编排执行

> 沉淀自多次长任务踩坑复盘（融资演讲稿、可研报告、尾气治理项目、北交所调研、图片化中断等）。核心目标：让「多源调研 + 文档生成 + 交付」这类长任务**一次跑通、不卡死、不丢结果、不图片化中断、不重复交付**。

## 一、触发条件

满足任一即启用本技能：

- 任务预计 **> 5 分钟**，或需**多源联网调研**（多维度/多产品/多政策对比）
- 需**生成或重构文件**（docx/xlsx/pptx/pdf/md）并交付给用户
- 需**拆分多个子任务并行执行**（调研、撰写、整合、交付分阶段）
- 单会话上下文可能膨胀导致**图片化中断**的复杂长流程

不满足时（单步、短任务、纯闲聊）直接用普通工具，不套本流程。

## 二、核心铁律（执行前默念）

1. **先确认范围**：增量 vs 全量、交付格式、**唯一基准文件是哪个**（任务前锁定，中途不再换）。
2. **子 agent 结果必须落盘**（`/tmp/xxx.txt`），整合时只 `read` 文件，**绝不依赖 completion event 文本**。
3. **等待只看"产物文件是否齐全"，不看"完成事件数"**（完成事件只来一次，数错就死等）。
4. **数据不用 Kimi/聚合摘要**，关键数字 `web_fetch` 官方原始出处（gov.cn、新华社、路透、行业协会官网）交叉验证后使用。
5. **文档生成用 `shutil.copy(基准)` + 替换段落文本**，严禁 `Document()` 空建 + `add_paragraph()`；中文字体同时设 `w:ascii/w:hAnsi/w:eastAsia`。
6. **邮件只在最终完成后发一次**（走 `send_email_once.py` 按文件名去重）。
7. **交付前重新打开验证**（段落数/字符数/格式/zip 完整性）。
8. **每步写状态文件**，支持断点续传。
9. **控制单次工具输出**：exec 用 `head/tail/grep` 分段，read 用 `offset/limit` 分块，避免长输出触发"图片化"。

> 图片化根因与应对见第六节（本质是渲染/回传层环境 bug，不是输出太长单一原因）。

## 三、任务书模板（spawn 子 agent 必须含 5 要素）

```
【目标】一句话说清这个子 agent 要产出什么（明确是"结果文件"而非"过程说明"）
【数据源】不用 Kimi，用 web_fetch 抓权威源；每个关键数字标注来源 + 置信度
【落盘】结果必须写入 /tmp/<维度>.txt（强制，不写文件就视为失败）
【验证标准】结果需包含哪几项关键数据/结论（可核对的硬指标）
【完成信号】写文件后返回"已写入 <路径> + 字节数 + 是否非空"
```

- 用 `sessions_spawn(mode="run")` 并行启动，记录每个 `childSessionKey`。
- 子任务指令里**强制要求"结果写文件 + 返回字节数"**，让完成信号可校验（字节数 > 0 才算真产出）。

## 四、可靠等待机制（卡死高发区，最关键）

- ❌ **不要数 completion event**——完成事件只来一次，把已完成的当 pending 再 `yield` = 死等（曾空转 27 分钟）。
- ✅ **每收到一个完成事件，就 `ls -la` 重查产物文件清单**：预期 N 个 `/tmp/*.txt`，N 个都在且**字节数 > 0** → 全部完成，立即进整合，不再 `yield`。
- ✅ **缺哪个才等哪个**；用 `sessions_yield` 等待，但**从不因为"觉得还差几个"而无限等**。
- ⏱️ **超时兜底**：某子 agent 超过预期时长（如 15 分钟）文件仍未出现 → 主动重查 `ls` → 仍缺则重试该子任务或直接报告主 agent，不干等。
- 📣 **进度通知**：距上次报进度 > 8 分钟仍无实质进展 → 主动发一条进度；每个阶段完成各报一次。
- 🧾 **整合前最后一遍全量核对**：`ls` 确认所有预期文件到位 → `head -c 100` 抽查非空 → 缺项列出清单，逐项补齐，不要带病整合。

## 五、完整流程（8 步）

1. **接收 + 范围确认**：增量/全量、交付格式、唯一基准文件；写 `任务_<名>_state.json`（phase=research）。
2. **拆分**：调研阶段并行 spawn N 个维度 agent；撰写/整合单 agent；交付单 agent。
3. **并行调研**：按第三节任务书 spawn；每个子 agent 结果落盘到独立 `/tmp/*.txt`。
4. **可靠等待**：按第四节查文件齐全度（`ls` + 字节数），齐全即进整合。
5. **整合**：单 agent `read` 所有文件，校验非空/含关键数据，汇总成统一口径。
6. **生成**：`shutil.copy(基准)` + 替换段落文本；中文字体设 ascii/hAnsi/eastAsia。
7. **验证**：重新打开文件，核对段落数/字符数/字体字号/缩进行距/zip 完整性。
8. **幂等交付**：云文档（同任务更新同一 doc，不 create 新 doc）+ 文件附件（MD/DOCX 分条发，不塞一条消息）+ 邮件仅终版一次。

## 六、"图片化"应对（已确认根因）

- **现象**：会话上下文变大后，工具输出被转成图片，上下文显示 `(see attached image)`，纯文本模型读不到内容。
- **根因**：渲染/回传层**环境级 bug/回归**，**无独立阈值配置**；会话级状态触发（严重时连 7 字节输出都会图片化）。
- **载体**：图片是**内嵌 base64**（`{type:"image",data:"<base64>"}`），随 transcript JSON 持久化，**不落独立 PNG 文件**（所以文件系统里找不到图，只能靠文本旁路）。
- **应对**：
  1. **治本**：`contextPruning(mode=cache-ttl, ttl=5m)` + `compaction(mode=safeguard, keepRecentTokens=20000)` + 主动 `/compact`，控制上下文规模。
  2. **治标**：控制单次输出（分块读、head/tail/grep）；大上下文下沉子 agent；图片/OCR/截图理解走**视觉桥**（视觉模型读图 → 转文字回主模型）。
  3. **应急**：已图片化 → `/new` 开新会话，关键结论先落盘再重建上下文。
  4. **兜底**：子 agent 结果写文件 + completion event 文本，双通道保底。

## 七、断点续传

`任务_<名>_state.json` 最小字段（每完成一个阶段更新一次）：

```json
{
  "phase": "research|integrate|generate|deliver|done",
  "baseFile": "/path/to/基准.docx",
  "format": "docx|md|xlsx|pptx|pdf",
  "subtasks": [
    {"dim": "硫资源", "file": "/tmp/research_sulfur.txt", "status": "done"}
  ],
  "expectedFiles": ["/tmp/research_sulfur.txt", "/tmp/research_geo.txt"],
  "delivered": {"docToken": "", "emailSent": false}
}
```

主会话重启后第一件事：**读状态文件 → 判断 phase → 从断点继续**，不重新拆分、不重复交付。`expectedFiles` 是等待与续传的唯一事实来源。

## 八、常见坑表

| # | 坑 | 根因 | 修复 |
|---|---|---|---|
| 1 | 等待卡死 | 数 completion event 死等 | 查产物文件齐全度（ls + 字节数） |
| 2 | 结果回传丢失 | 依赖 completion event 文本 | 结果强制落盘，整合只 read 文件 |
| 3 | 空文档/格式乱 | `Document()` 空建 + `add_paragraph()` | `shutil.copy` 基准 + 替换段落文本 |
| 4 | 多 agent 覆盖同一文件 | 未锁定基准，来回换 | 任务前锁定唯一基准，中途不换 |
| 5 | 数据虚构/混入推演 | 用 Kimi/聚合摘要 | web_fetch 权威原始源交叉验证 |
| 6 | 邮件乱发 | 每版都发 | 只终版发一次 + `send_email_once.py` 去重 |
| 7 | 图片化中断 | 会话级环境 bug，无独立阈值 | compaction/pruning + 分块读 + 拆子 agent + 视觉桥 |
| 8 | 断点丢失 | 状态不落地 | 每步写 state.json，重启先读状态 |
| 9 | 云文档版本堆一堆 | 每迭代 create 新 doc | 同任务更新同一 doc token |
| 10 | key/token 截断 | 复制了掩码显示的 `…` | 用完整 key，`…` 是显示掩码非真实值 |
| 11 | 子 agent 自述与实际不符 | 声称完成实际空文件 | 交付前重新打开验证字节数/段落数 |
| 12 | 中文渲染异常 | 只设西文字体属性 | 同时设 `w:ascii/w:hAnsi/w:eastAsia` |
| 13 | 目录页码手打失效 | 手打目录无自动更新 | 插入 Word TOC 域代码 + 标题 `w:outlineLvl` |
| 14 | heredoc 转义报错 | Python 把 `\o` 当转义 | 含 `\` 用 `r'...'` 原始串或写 .py 文件执行 |

## 九、成功标准（Done）

- [ ] 状态文件 `phase=done`
- [ ] 产物文件非空、通过交付前验证（段落数/字符数/格式/zip）
- [ ] 云文档已上传 + 所有权已授用户
- [ ] 邮件已发（仅一次，有去重日志）
- [ ] 复盘已写入 TOOLS.md（把新踩的坑沉淀下来）
